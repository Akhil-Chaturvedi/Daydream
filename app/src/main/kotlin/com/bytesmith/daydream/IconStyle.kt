package com.bytesmith.daydream

/**
 * Shared constants for notification icon display styles.
 * Used by both MainActivity (settings) and DreamService (display).
 */
object IconStyle {
    const val SYSTEM = 0
    const val MONOCHROME = 1
    const val OFF = 2
}
